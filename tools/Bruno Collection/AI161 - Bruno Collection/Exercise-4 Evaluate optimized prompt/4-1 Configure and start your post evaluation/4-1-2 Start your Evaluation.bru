meta {
  name: 4-1-2 Start your Evaluation
  type: http
  seq: 2
}

post {
  url: {{AI_API_URL}}/v2/lm/executions
  body: json
  auth: bearer
}

headers {
  AI-Resource-Group: default
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "configurationId": "{{postEvalConfigurationID}}"
  }
}

script:post-response {
  let data = res.getBody();
  if (data.id) {
    let execID = data.id;
    // https://docs.usebruno.com/testing/script/javascript-reference#setenvvar
    bru.setEnvVar("postEvaluationID", execID);
    console.info("Set postEvaluationID to:", execID);
  }
}
